# Copy to .env.local and fill in values. Never commit .env.local to git.

VITE_APP_ENV=local
VITE_APP_NAME="CRM Dermacells"

# Sentry (web PWA)
# Package required: @sentry/react (see apps/web/src/sentry.ts)
VITE_SENTRY_DSN_WEB=
VITE_SENTRY_TRACES_SAMPLE_RATE=0.05
VITE_SENTRY_RELEASE=
